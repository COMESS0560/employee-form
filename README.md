# Week 2 Creating an Employee Form

This React project creates an Employee Form with fields for name, email, and phone number. The form uses state to track input values, handles input changes, handles form submission, logs the new employee to the console, clears the fields after submission, and adds the employee to a list.

## Run the project

```bash
npm install
npm start
```
